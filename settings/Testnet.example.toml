[network]
name = "testnet"
stacks_node_rpc_address = "https://api.testnet.hiro.so"
deployment_fee_rate = 10

[accounts.deployer]
# Encrypt your mnemonic via `clarinet deployments encrypt` and paste the cipher below.
# Never commit real plaintext seed phrases.
mnemonic = "<ENCRYPTED TESTNET MNEMONIC HERE>"
