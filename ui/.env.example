# UI environment template
# Copy to ui/.env and adjust per deployment target.

# "testnet" or "mainnet"
VITE_STACKS_NETWORK=testnet

# Hiro API endpoint (defaults to public Hiro nodes if empty)
# For testnet: https://api.testnet.hiro.so
# For mainnet: https://api.hiro.so
VITE_STACKS_API_URL=

# Contract metadata. Keep in sync with Clarinet deployment.
# Replace with your deployed contract address for the selected network.
VITE_CONTRACT_ADDRESS=ST1B3AYKVPXY4MZXWPKNGHGYGRDP3AFKG19Q0YD2Q
VITE_CONTRACT_NAME=hello-world
VITE_CONTRACT_IDENTIFIER=${VITE_CONTRACT_ADDRESS}.${VITE_CONTRACT_NAME}

# Optional Chainhook/webhook endpoint for status card
VITE_CHAINHOOK_WEBHOOK_URL=http://localhost:4000/hooks/hello-world

# Gemini API key for Clarity Assistant (leave blank to disable)
VITE_GEMINI_API_KEY=