// Legacy combined suite — intentionally skipped. All active coverage lives in per-contract test files.

import { describe, it } from "vitest";

describe.skip("legacy contracts suite (use per-contract tests)", () => {
  it("placeholder", () => {});
});
